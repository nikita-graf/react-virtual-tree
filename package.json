{
  "name": "react-virtual-tree",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "dev": "./node_modules/.bin/webpack-dev-server --config webpack.config.js --content-base src --display-reasons --display-chunks --progress --hot --port 8888",
    "build": "./node_modules/.bin/webpack --config webpack.config.js --build --display-reasons --progress --bail",
    "gh-pages-folder": "npm run build && mv dist gh-pages && cp example/* gh-pages && cp css/* gh-pages",
    "gh-pages": "npm run gh-pages-folder && git checkout gh-pages && git add gh-pages && git commit -m 'upd' && git subtree push --prefix gh-pages origin gh-pages && git checkout master"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-core": "^6.7.2",
    "babel-loader": "^6.2.4",
    "babel-plugin-transform-class-properties": "^6.6.0",
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-react": "^6.5.0",
    "webpack": "^1.12.14",
    "webpack-dev-server": "^1.14.1"
  },
  "dependencies": {
    "bem-cn": "^1.2.2"
  }
}
